const plusEqual = number => {
  number = number + 1
  number++
  number += 1
  number = number - 1
  number --
  number -= 1
  return number
}

const secondPlusEqual = number => {
  number = number + 1
  number++
  number += 1
  number = number - 1
  number --
  number -= 1
  return number
}

const thirdPlusEqual = number => {
  number = number + 1
  number++
  number += 1
  number = number - 1
  number --
  number -= 1
  return number
}
